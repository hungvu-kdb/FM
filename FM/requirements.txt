# Runtime dependencies
# Native face stack (dlib) is required only at runtime for the Encoder.
# It is intentionally NOT imported at package import time so pure-logic
# tests can run without it.
face_recognition==1.3.0
numpy==2.4.4
Pillow==12.2.0

# Development / testing dependencies
pytest==9.1.0
hypothesis==6.155.3
